name: Release

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust artifacts
        uses: Swatinem/rust-cache@v2

      - name: Build release binary
        run: cargo build --release

      - name: Package binary (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          mkdir -p dist
          asset_name="what-the-cert-${{ runner.os }}-${{ runner.arch }}"
          cp target/release/what-the-cert "dist/${asset_name}"
          tar -czf "dist/${asset_name}.tar.gz" -C dist "${asset_name}"
          rm "dist/${asset_name}"

      - name: Package binary (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path dist | Out-Null
          $assetName = "what-the-cert-${{ runner.os }}-${{ runner.arch }}"
          Copy-Item target/release/what-the-cert.exe "dist/$assetName.exe"
          Compress-Archive -Path "dist/$assetName.exe" -DestinationPath "dist/$assetName.zip"
          Remove-Item "dist/$assetName.exe"

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: what-the-cert-${{ runner.os }}-${{ runner.arch }}
          path: dist/*

  publish:
    name: Publish GitHub release
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Publish release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*
          generate_release_notes: true
